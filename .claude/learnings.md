# Pilcrow v1 — Agent Learnings

Append-only. Format: `- [YYYY-MM-DD] [agent or area]: [the lesson, one sentence]`

- [2026-04-29] typography-architect: Pretext's `continueSoftHyphenBreakableSegment` packs graphemes after a soft-hyphen, so the materialized line can end with `-<suffix>` (e.g. `ital-i`) not just `-`; orphan detection must handle both cases, not only lines that end with a bare `-`.
- [2026-04-29] typography-architect: Stripping SHYs in document order (first SHY first) causes collateral grapheme-break regressions on unrelated words; always use targeted stem-search (`stem + U+00AD`) to find the specific SHY to strip.
- [2026-04-29] typography-architect: The orphan guard helpers (`guardFlat`, `guardRich`) must be defined inside `page.evaluate()`'s callback — they run in the browser context and cannot be imported as Node modules; keep them co-located in `playwright.ts`.
- [2026-04-29] typography-architect: When adding `<cite>` (pull quote attribution) to the HTML, Hyphenopoly will hyphenate author names inside `<cite>` unless `<cite>` is explicitly added to the skip list in `hyphenate.ts` alongside `<code>` — author names must never be hyphenated.
- [2026-04-30] design-critic: Screenshot pattern-matching for hyphenation orphans is unreliable — a hyphenated suffix at line-end (e.g. `prim-`, `destina-`, `genu-`, `walkRichIn-`) reads visually as a standalone fragment but is actually the tail of a 100%+ fill line; char-count `.pt-line` widths is the authoritative method (two incidents: `walkRichIn-` on `inline-markup.md` ¶3, and five false-positive orphans across all posts — all resolved as full-fill lines under char-count inspection); design-critic.md Critical rule 7 now mandates char-count first.
- [2026-04-29] typography-architect: The `splicePostBody` regex in `pilcrow-typeset.ts` used a non-greedy `[\s\S]*?` match that stops at the FIRST `</div>` inside `.post-body`; any primitive that introduces a nested `<div>` inside the post body (e.g. the `.footnotes-mark` pilcrow glyph element) will break it — replace with a depth-counting balanced-div scanner, not a regex.
- [2026-04-29] typography-architect: CSS `::before { content: 'U+glyph' }` with Unicode characters (¶, *, etc.) is read aloud by NVDA ("pilcrow"), JAWS, and VoiceOver when verbose; always use a real DOM element with `aria-hidden="true"` for decorative Unicode glyphs instead of generated content.
- [2026-04-29] typography-architect: Astro includes `remark-gfm@4.0.1` by default with `gfm: true` — GFM footnotes work without adding it to `astro.config.mjs`; do not add it as an explicit plugin as it will run twice.
- [2026-04-30] typography-architect: CSS Grid grid-column can only target direct children of the grid container — an `<aside>` nested inside `<span>` inside `<p>` cannot use grid-column directly; use float + negative margin-right (`calc(-width - gap)`) instead, which is the classical magazine-float technique and works with no JS.
- [2026-04-30] typography-architect: When `.post-body` gains a Grid wider than the `.post` wrapper's `max-width: 65ch`, the right-margin column overflows or is clipped; the fix is to expand `.post` to `max-width: calc(65ch + gap + sidenote-width)` at the relevant breakpoint in a `@media (min-width: breakpoint)` rule — not by removing the prose max-width.
- [2026-04-30] typography-architect: The `readMeasurementCSS()` fallback for `postBodyMaxWidth` is `'65ch'` — if `.post-body` loses its `max-width` property (e.g. after Grid restructuring), the fallback still correctly constrains body paragraphs in the loader page to 65ch; no change to the extraction logic is needed as long as the fallback matches the prose column width.
- [2026-04-30] typography-architect: Astro 6 content collections (glob loader) use a persistent data store cache at `node_modules/.astro/data-store.json` (devalue-serialized) — when the digest of a source file hasn't changed, Astro returns the cached rendered HTML and NEVER calls the markdown processor; a rehype plugin that appears not to execute is almost always a cache hit, not a pipeline bug; clear `node_modules/.astro/data-store.json` (not just `.astro/` or `node_modules/.vite`) to force a fresh render.
- [2026-04-30] typography-architect: Errors inside rehype plugins running in Astro's content-layer glob loader are caught silently by the loader's try/catch and logged only as `[glob-loader] Error rendering X.md: ...`; the build succeeds but `entry.rendered` is set to `undefined`, producing empty post bodies — always run a full clean build with `node_modules/.astro/data-store.json` deleted when debugging plugin execution.
- [2026-05-01] typography-architect: npm strips files named `.gitignore` when packing the package — when bundling a template directory inside an npm package, store the template's gitignore as `gitignore` (no leading dot) and have the CLI rename it to `.gitignore` when copying into the user's target directory; affects any `create-*` npm package that needs to ship a `.gitignore` in its template.
- [2026-05-01] typography-architect: `packages/` directory inside an Astro project is automatically ignored by Astro's build pipeline (Astro only processes `src/`, `public/`, and configured directories) — no monorepo workspace config needed when keeping a sibling create-package alongside the main Astro project.
- [2026-05-01] typesetter: Sidenote markers (`<sup class="sidenote-marker">`) appended AFTER `display:block` `.pt-line` spans inside `<p>` trigger CSS anonymous block wrapping, rendering the marker as a standalone element on its own visual line ("stray 1 in accent colour"); fix is to inject the marker HTML inside the last `.pt-line` span via `buildLineSpansHTML(inners, markers)` helper in `playwright.ts`.
- [2026-05-06] typesetter: Astro 6 + `moduleResolution: 'Bundler'` does NOT auto-pick up `@types/node` from a workspace-hoisted devDep alone — explicit `"types": ["node"]` in the package's `tsconfig.json` was required alongside the devDep declaration to resolve `node:fs/promises`, `process`, etc. (Discovered during `pilcrow-typeset` extraction.)
- [2026-05-06] typesetter: `unist-util-visit` v5 with hast trees passes `Root | Element | undefined` as the `parent` argument to the visitor callback, NOT `Parent | null`. The codebase had typed it as `Parent | null` since the original write — the typed package extraction surfaced the mismatch. Pattern for any future `visit()` callback against a hast tree: import `Root` and `Element` from `'hast'`, type parent as `Root | Element | undefined`, and guard with `parent === undefined`.
- [2026-05-06] typesetter: Sprint extraction sub-tasks can land their work eagerly during file moves rather than as discrete steps — always read the current state of the target file before planning a write, the spec may already be satisfied. Cosmetic churn against an API-locked surface is worse than no edit. (Verified during `pilcrow-typeset@0.1.0` sub-task 3: `typeset()` and four re-exports were already in place from sub-tasks 1–2; correct response was to verify line-by-line and confirm gates, not rewrite.)
- [2026-05-06] typesetter: When verifying that a build-time text transformation actually ran (Hyphenopoly soft-hyphen injection, or any pass that emits markers a downstream consumer may eat), grep the entire `dist/` corpus, not a single representative file. A single-post grep can return zero matches even when the transformation worked correctly, because every emitted marker can land at a break position where the downstream pass (in this case pretext) consumes it back out. Corpus-wide grep is the only reliable signal. (Discovered during sub-task 4 verification: `dist/posts/the-cheapest-signal/index.html` had 0 U+00AD characters and almost triggered a false-positive "stop and report"; all-posts grep surfaced 20 surviving SHYs across the corpus, plus mid-syllable line breaks in every post — the actual proof.)
- [2026-05-06] typesetter: Sprint-shaped work (multi-deliverable, time-boxed, sub-tasks under ~30 minutes each) does not need per-sub-task feature specs. The sprint plan itself functions as the spec. Reserve `context/feature-specs/NN-<name>.md` for unsprinted standalone features where the planning artefact and the spec artefact would otherwise be the same document written twice.
- [2026-05-06] typesetter: Cross-package sibling-relative imports (e.g. `'../../pilcrow-typeset/dist/index.js'`) work for monorepo dev where the consuming package's dist/ is co-located with the dependency's dist/, but break the moment the consuming package is consumed via `npm install` — the relative path is preserved verbatim in the published tarball and resolves to nothing in the consumer's `node_modules` layout. For published sibling packages within the same monorepo: declare the dep explicitly via the package name in `dependencies`, and use `import { X } from '<package-name>'`. For local dev pre-publish, declare the dep as `"file:../<package>"` in `dependencies` (single declaration only — see next learning). Mechanically swap `file:..` → `^<version>` via `npm pkg set` immediately before `npm publish`, then revert after — Stream D's "swap-publish-revert" sequence in PILCROW_V3_SPRINT_PLAN.md captures the pattern.
- [2026-05-06] typesetter: Bun's resolver does NOT treat a `devDependencies` `"file:..."` entry as an override of a duplicate `dependencies` `"^x.y.z"` entry. It warns about the duplicate declaration, then attempts registry resolution of the `dependencies` entry first, failing with a 404 if the package isn't on npm yet. This may differ from npm/pnpm/yarn behaviour — do not assume. Use a single declaration in `dependencies`; switch the version specifier between `file:..` (local dev) and `^x.y.z` (publish) at publish time rather than dual-declaring.
- [2026-05-06] typesetter: Astro 6 emits inline `<script type="module">` blocks inside `.astro` files verbatim — bare specifiers like `import * as pretext from '@chenglou/pretext'` will 404 in the browser because no import map is generated. Pretext (and any other client-bundled module) must live in a separate `.ts` file imported via a non-`is:inline` `<script>` tag from the .astro file; that's the only way Vite resolves the bare specifier and emits a real client chunk.
- [2026-05-06] typesetter: Astro 6 treats files matching `src/pages/_*.astro` as private and does not build them — using an underscore-prefixed filename for a "hidden" spike route makes a real-browser load impossible. Use a non-prefixed filename + `<meta name="robots" content="noindex,nofollow">` + an explicit sitemap-filter exclusion in `astro.config.mjs` instead.
- [2026-05-06] typesetter: For a temporary route to be excluded from the sitemap, add a `filter:` clause to `@astrojs/sitemap` config that returns `false` when the URL contains the route slug — and document the deletion path inline (e.g. "delete this clause when the spike file is removed") so the workaround does not outlive its purpose.
- [2026-05-06] typesetter: Pretext relies on `Intl.Segmenter` for grapheme/word segmentation; Firefox 125+ (April 2024) is the gate. Detect with `typeof Intl?.Segmenter === 'function'` and ship a fallback notice rather than a silent failure when running pretext in client code.
- [2026-05-06] typesetter: `@chenglou/pretext@0.0.6` ships at ~16 kB gzipped to the client bundle and has zero transitive runtime deps — it's cheaper than most analytics scripts and there is no architectural reason to gate it behind a hosted backend.
- [2026-05-06] typesetter: `await document.fonts.ready` is mandatory before any pretext call in the browser — same gate `playwright.ts` uses. Without it, canvas `measureText` returns metrics for the fallback system font and pretext breaks lines for the wrong typeface; the symptom is a visually correct page that diverges from the deployed pretext output by 1–3 px per line.
- [2026-05-06] typesetter: To derive the canvas font shorthand and lineHeight in the browser exactly the way `playwright.ts` does, attach a probe `<p>` inside `.post-body`, read `getComputedStyle()`, then build `${cs.fontStyle} ${cs.fontWeight} ${cs.fontSize} ${quotedFirstFamily}`. CSS `line-height: 1.7` resolves to a px value via `parseFloat(cs.lineHeight)` — no manual multiplication needed in modern browsers.
- [2026-05-06] typesetter: Browser canvas + Playwright canvas use the same `OffscreenCanvas.measureText` + `Intl.Segmenter` path, so output is byte-identical when inputs match. The three structural differences between a naive in-browser pretext call and the deployed pipeline are: column width (must come from `--prose-measure` in `global.css`, not the visual element width), drop-cap float-aware narrowing (must call `layoutNextLineRange` per line and shrink width while `y < capHeight`), and SHY pre-pass (Hyphenopoly's soft-hyphen injection must run before pretext sees the text).
- [2026-05-06] typesetter: The installed `hyphenopoly@6.1.0` package's main entry (`hyphenopoly.module.js`) uses `WebAssembly` + `TextDecoder` and a fully configurable async `loader(file, baseURL)` — no Node-specific imports — so it CAN run in a browser if (a) the package is moved from devDependencies to dependencies (so Vite bundles it), (b) `en-gb.wasm` is shipped at a public URL like `/_hyphenopoly/en-gb.wasm`, and (c) the loader is rewritten to `fetch().then(r => r.arrayBuffer())`. The companion `Hyphenopoly_Loader.js` is NOT a programmatic API — it's a `hyphens: auto` CSS polyfill that mutates the DOM by class selector and cannot be invoked synchronously over a string. So any browser SHY pre-pass must use `hyphenopoly.module.js` with a custom fetch loader, not `Hyphenopoly_Loader.js`.
- [2026-05-06] typesetter: `hyphenopoly@6.1.0` ships only `<lang>.wasm` files (e.g. `en-gb.wasm` at 33 kB raw / 22 kB gzipped) — there are NO separate `.hpb` pattern files. Pattern data is embedded in the wasm. The loader callback only ever fetches `<lang>.wasm`. Don't waste time looking for `.hpb` to ship.
- [2026-05-06] typesetter: First-typeset Hyphenopoly init in browser (lazy-loaded wasm fetch + wasm instantiation + en-gb language registration) is ~6–20 ms in headless Chromium. Cheap enough to gate behind the first `typeset()` call rather than eager-loading at import time — the latency disappears against the typesetter's own ~30 ms work and never blocks initial paint.
- [2026-05-06] typesetter: BrowserRenderer step 3(a) (`resolveProseMeasurePx()` reading `--prose-measure`) is necessary as a hard-error invariant check, but NOT sufficient as the per-paragraph layout width — `playwright.ts:930` uses `p.clientWidth` per paragraph, which is the correct semantics for narrowed containers (`aside.sidenote 25ch`, `.pullquote 50ch`, `.footnotes p`). Using a single `--prose-measure`-derived value across all paragraphs causes BrowserRenderer to lay out sidenote and pull-quote paragraphs at full prose width, breaking parity with the production pipeline. Mirror the production semantics: `resolvedWidth = options.maxWidth || p.clientWidth || proseMeasurePx` per paragraph.
- [2026-05-06] typesetter: BrowserRenderer acceptance gate against deployed `pilcrow.page` is two-axis: (a) BrowserRenderer ≡ local-built dist HTML (proves byte-identical to current production pipeline), (b) BrowserRenderer ≡ deployed HTML (proves the deployed site is current). When (a) passes but (b) fails, the divergence is a stale deploy, not a renderer bug. Always run BOTH comparisons and report which axis diverged. If only (b) fails, redeploy is the fix; BrowserRenderer is correct.
- [2026-05-06] typesetter: Hyphenopoly v6 embeds language pattern data inside the `<lang>.wasm` binary; there is NO companion `.hpb` file shipped with the package. Earlier sprint plans expected to ship `en-gb.hpb` alongside `en-gb.wasm` — that expectation is wrong, only the wasm needs to land in `public/_hyphenopoly/`. Verified `find node_modules/hyphenopoly -name '*.hpb'` returns empty; only `<lang>.wasm` files exist.
- [2026-05-06] typesetter: `resolveProseMeasurePx()` returning a single number for all paragraphs is structurally wrong — production `playwright.ts:930` reads `p.clientWidth` per paragraph, which gives sidenote-narrowed (25ch), pullquote-narrowed (50ch), footnotes-narrowed widths their correct layout target. BrowserRenderer must mirror that exactly: `resolvedWidth = options.maxWidth || p.clientWidth || proseMeasurePx` (the `--prose-measure` invariant check is retained as fallback / sanity rail, not as the source of truth).
- [2026-05-06] typesetter: The 2026-04-29 acceptance-gate rule "compare against deployed" is symmetric — it catches drift in either direction. The inverted-asymmetry case (current local is correct, deployed is stale) reads superficially like a gate failure, but the resolution is to redeploy and re-run, not to relax the rule. The rule itself is sound; the asymmetry is in the artefact under test, not the methodology.
- [2026-05-06] typesetter: BrowserRenderer gate uncovered Linux Playwright Chromium 147 silently failing to load variable-axis TTF fonts in headless contexts despite bytes being delivered with correct MIME and CORS. Symptom: rendered glyph widths returned as clean integers (10, 170, 264) on Linux vs sub-pixel-precise floats (9.8378, 168.077, 263.713) on macOS arm64. FONTS-API-PROBE confirmed FontFace.status: 'loaded' on Linux despite no actual rendering. Diagnosis required parallel-family probe with both variable and static instances under throwaway @font-face declarations. Fix: switched body type pipeline from variable Fraunces to four static 144pt opsz instances (Regular 400 / SemiBold 600 / Bold 700 / Italic 400 italic). Drop-cap weight changed 500 → 600 because static release has no weight 500 (variable surface synthesised it from the wght axis); SemiBold preserves drop-cap-vs-body weight distinction. Residual ~3% line-count drift on long posts (>100 pt-lines) remains due to genuine CoreText-vs-FreeType sub-pixel rasterisation differences — documented as a known limitation, candidate for v1.x Linux-build-container approach. Asset surface shrank 803 KB variable → 389 KB static. The 2026-04-29 deploy-comparison rule's restatement: BrowserRenderer must match local Playwright dist/ byte-for-byte; deployed pilcrow.page may drift up to +3 lines on long posts. CF Pages build cache also surfaced as a stale-output source during this arc — clearing via dashboard + empty-commit-redeploy resolves. Wrangler is the right tool for verifying CF Pages deployment status; installed during this work.
- [2026-05-06] typesetter: Diagnostic harnesses must inherit from production CSS, not duplicate it. The spike-browser-renderer-gate page hard-coded its own Google Fonts <link> during the original spike, which became invisible drift when the production fix swapped fonts to self-hosted statics — the gate then measured BrowserRenderer against the OLD variable-Fraunces baseline (114 lines) instead of the new static baseline (102 lines), producing a false-fail of 114-of-114. Pattern to remember: any harness that compares X against canonical-Y must reach canonical-Y by reference, never by reproduction. If you find yourself copy-pasting font links, build commands, or measurement-critical CSS into a diagnostic, that copy is a future false-positive waiting to happen. The fix here was deleting six lines from one page; the lesson is wider — never reproduce production state inside diagnostic infrastructure.
- [2026-05-06] typesetter: document.fonts.ready resolves based on which @font-face declarations have been REQUESTED (i.e. needed by laid-out content), not which have been DECLARED. If a weight/style is only used by content that's about to be programmatically injected (e.g. a drop-cap span styled with font-weight: 600 when no other element on the page uses 600), document.fonts.ready can resolve while that weight is still unrequested. Subsequent getBoundingClientRect on the injected element falls back to the closest loaded weight rendered with synthetic transformations — measurably different geometry than the real cut. BrowserRenderer hit this for SemiBold 600 cap measurement (78.64px synthetic-bold rect vs 71.14px correct rect). Fix: explicit Promise.all([document.fonts.load('600 87.4px Fraunces'), document.fonts.load('400 19px Fraunces'), ...]) for every declared weight/style after document.fonts.ready, before any injection-and-measure path. playwright.ts didn't hit this naturally because its per-paragraph processing interleaved enough time for lazy font fetches; a single-shot measurement path won't. Diagnostic tell: the cap rendered as a square (78.64×78.64) instead of an advance-width-driven rectangle (71.14×78.64) — synthetic-bold widening tends to push glyphs toward the line-height square because the synthesis is uniform-axis.
- [2026-05-06] typesetter: When a structural side-by-side comparison surfaces multiple deltas (e.g. position:absolute vs static, visibility:hidden vs visible, body padding, viewport size), the most-different-looking delta isn't necessarily the causal one. The cap-measurement debug round identified four genuine structural differences between BrowserRenderer's host setup and playwright.ts's loaderHTML; the actual cause was below all of them, in the font-loading lifecycle. The corrective signal came from the extended probe — both renderers reporting display: block + float: left identically despite different rects — which forced 'cause is below the cap's own layout rules' as the conclusion and redirected to font-loading. Lesson: structural reads are necessary but not sufficient. When a probe matrix shows 'identical CSS, different rect', look at the lifecycle (when was the element created, when was measurement called, what async work might still be pending) before the structure. Defaults to suspect: font loading, image decoding, layout-shift settling, microtask flush ordering.
- [2026-05-07] typography-architect: CF Pages can show wrangler-reported 'Active' status before new content is actually serving; deploy-to-served gap can be ~5min on cold deploys. Verify by polling actual response content size, not deploy status.
- [2026-05-07] typography-architect: Astro's `requestfinished` Playwright instrumentation reports timing.responseEnd minus timing.requestStart in milliseconds — useful for first-load network probes. Sub-task 7 verified Fraunces TTF fetches at 2-8ms each on `bun run preview` (localhost) and en-gb.wasm at 1ms. Real-world CF Pages numbers will differ; localhost numbers are useful as a "everything was requested in parallel and resolved fast" sanity check, not as a production benchmark.
- [2026-05-07] typography-architect: Async coalescing pattern for live-preview typeset: when overlapping events can arrive faster than typeset() completes, use a single `pendingMarkdown: string | null` slot + `typesetting: boolean` flag rather than a queue. Drop intermediate states (we only want the LATEST source rendered, not every keystroke between the user's start and stop) and re-enter `runTypeset()` from the `finally` block of the in-flight call. Saves typesetter cycles on rapid input and guarantees the preview always converges to the latest editor state.
- [2026-05-07] typography-architect: For decoupled component communication in Astro inline scripts, `document.addEventListener('pilcrow:custom-event', …)` + `document.dispatchEvent(new CustomEvent(…, { detail: {…}, bubbles: true }))` is enough — no pubsub library needed. The contract is the `detail` shape, documented at the dispatch site. Sub-task 7 picked this over a shared state object because: (a) two components, one event direction, no need for state sync; (b) future dispatchers (settings, share-URL) can land independently without import-coupling to existing components; (c) testable from Playwright via page.evaluate without any framework.
- [2026-05-07] typography-architect: Astro inline `<script>` tags become bundled deferred ES modules and execute in document order. A consumer module that uses a top-level `import` (e.g. `import { BrowserRenderer } from './…'`) attaches its `addEventListener` only AFTER its import resolves, which is at least one microtask later than a producer module that uses no top-level imports. So a producer that dispatches via `queueMicrotask` will fire BEFORE the consumer's listener attaches and the dispatch will be missed. Two robust fixes: (a) producer dispatches via `setTimeout(0, 0)` to yield a full macrotask; (b) producer also writes the latest payload to a `window.__name` mirror that late attachers can read synchronously on init. Sub-task 6 used both belt-and-braces. The `setTimeout(0)` alone is enough today, but the mirror future-proofs against any consumer that takes longer to register (e.g. dynamic imports, lazy hydration).
- [2026-05-07] typography-architect: Pre-flight asset verification BEFORE bulk-downloading saves wasted work. Six-font playground shortlist required checking 5 upstream repos for static TTF availability of 4 specific faces each. EB Garamond was the gotcha: `google/fonts/ofl/ebgaramond/` ships ONLY the variable TTF (`EBGaramond[wght].ttf`), not statics — but the upstream `octaviopardo/EBGaramond12/fonts/ttf/` ships the full Regular/Medium/Bold/Italic statics. Always verify the actual tree before downloading; don't assume "google/fonts has everything".
- [2026-05-07] typography-architect: Lazy-loading fonts via `document.fonts.load('weight size <Family>')` is the right primitive — it triggers fetch (if not cached) and resolves once the FontFace is ready. But CSS `@font-face` declarations alone trigger speculative browser fetch when the family is referenced in any rendered style — even via inline `style="font-family: 'Family'…"` on the swatch glyph. To keep the initial paint to Fraunces-only, the swatch glyphs must NOT carry the family in their initial styling. Set them to inherit (so they paint in the page's default Fraunces), then update `glyph.style.fontFamily` AFTER the user's first click triggers the load. Initial paint goes from 6 fonts × 1 weight (≈400 KB extra) down to 0 KB extra; per-font swatch then becomes a real specimen post-interaction.
- [2026-05-08] typography-architect: Structural verification (DOM contains expected elements, CSS bundle contains expected rules, JS bundle references expected names) is necessary but NOT sufficient for sub-tasks that ship visible UI affordances. Sub-task 6 (settings panel with six-font swatches) shipped to a "build clean, all gates green" state under structural-only verification — and shipped with a teaching-surface bug. All six font swatches rendered with identical fallback glyphs because (a) per-swatch `font-family` CSS rules keyed on `data-font` were missing on the `.font-swatch-glyph` element, and (b) only Fraunces was loaded at first paint. The font picker's whole job is to let a user *see* the difference between Fraunces and EB Garamond before clicking; six identical glyphs in the cascade fallback defeat the affordance entirely. Structural checks (six `<button class="font-swatch">` elements present, six `data-font` attributes correctly populated, `pilcrow:settings-changed` dispatching with the right detail shape) all passed.

  The fix (commit `49ceee7`) addressed both halves: per-swatch CSS rules `.font-swatch[data-font='Fraunces'] .font-swatch-glyph { font-family: "Fraunces", … }` for each of the six families, plus `eagerLoadSwatchSpecimens()` in the hydration script that issues `document.fonts.load('400 19px <Family>')` for all six Regular 400 cuts at page load and stylises each swatch as its load resolves. Cost: ~1.3 MB of additional initial payload (acceptable for a typography playground; the user is here specifically to compare faces). The deferred specimen-subset optimisation is documented inline.

  Heuristic for future work: if a sub-task ships UI that a user *looks at* to make a choice — a font picker, palette swatches, drop-cap previews, line-height demos, OG card thumbnails, before/after typesetting comparisons — structural verification does not close the loop. The verification surface must match the affordance surface. That means at least one of: (a) a screenshot diff against an expected baseline (visible regression caught mechanically); (b) a Playwright test that reads `getComputedStyle(...).fontFamily` on the rendered swatch and asserts it resolves to the swatch's declared family rather than the cascade fallback; or (c) explicit human visual confirmation, recorded in the session notes as "I looked at it, it works." Pick whichever costs less; never settle for "the DOM has the right elements" alone for affordances that exist to be seen. Six radio buttons can all dispatch the right events while looking identical to the user — and looking identical is the bug.

- [2026-05-08] typography-architect: Measurement-context bugs hide at boundaries between hosts. Engine validation in environment X means nothing for environment Y unless the host context is canonical-equivalent. Sub-task 10's −4 line gap (BrowserRenderer 14 lines vs PlaywrightRenderer 18 lines on inline-markup.md) was investigated as a renderer/host discrepancy — but the actual cause sat in `playwright.ts::extractCSSProp`, a regex-based CSS selector extractor that DOES NOT strip comments before matching. A documentation comment added in commit `945ae3a` (sub-task 6 settings panel work) contains the literal text `body { font-family: "Fraunces" … }` at `public/styles/global.css:58`. The regex `/body\s*\{([\s\S]+?)\}/g` matched that comment block FIRST (it appears before the real `body { ... }` rule at line 247), returning `font-family: "Fraunces" …` — including a literal U+2026 ellipsis character — as the extracted value. The loaderHTML then declared `body { font-family: "Fraunces" …; ... }`, which is invalid CSS; Chromium fell back to Times, the canvas measured glyphs against Times metrics (`measureText('e').width` 8.43px vs real-Fraunces 7.91px — ~6% wider), and every post built since `945ae3a` shipped with ~20% more line breaks than the typography intent (cheapest-signal 128 vs 102, sidenotes 87 vs 66, inline-markup 18 vs 14). Symptom appeared as a BrowserRenderer flaw because BrowserRenderer was the new component under test; reality was that BrowserRenderer was correct (real Fraunces in a real browser) and the build-time pipeline was the one drifting.

  Three lessons. (1) When comparing two paths and one path is "validated production" while the other is new, presume the new path may have surfaced a latent bug in the validated path — entry 4 (surface-reading bias) said this; entry 2 (BrowserRenderer-vs-deployed harness anti-pattern) said you can't trust validated production code under different conditions. Today's case is the symmetric companion of entry 2: validated build-time was the broken side, the new-component path was correct. (2) Probe BOTH paths with the same diagnostic shape; a side-by-side capture of `getComputedStyle(host).fontFamily` returning `"Times"` (build) vs `'Fraunces, ui-serif, …'` (browser) was the unambiguous signal — neither side alone would have made it visible. (3) Regex-based parsers of human-readable formats (CSS, HTML, Markdown) must strip comments before matching. The comment-shadow anti-pattern is well known but easy to miss when the comment is added months later by unrelated work. `extractCSSProp` had been correct from the original write through commit `945ae3a`; the latent bug was introduced not by changing the regex but by adding a CSS comment that the regex could not have known about. Fix: `stripCSSComments()` helper, one-line addition to `extractCSSProp`. Production line counts on every post drop ~20% — the typography is now what it always wanted to be.

- [2026-05-08] typography-architect: The orphan guard's scope is narrower than the user-visible symptom class it appears to address — fourth instance of the "every layer of the rendering pipeline must be audited for parity at every host" pattern (entries 2, 4, 6). Diagnostic round following Cowork's verification of the deployed `the-cheapest-signal` post: deployed serves 105 pt-line spans, local clean-build dist emits 102, both consistent with the documented ~3-line Linux/macOS sub-pixel rasterisation drift (entry 6); Cowork's visual count of 76 main-paragraph + 3 pull-quote = 79 was a methodology delta (Cowork counted only the body-prose lines, excluding the 17-paragraph total of pull-quote, sidenote, footnote-list and lede-region lines that the grep counts). The 102-line prediction from this morning's `a870f1e` fix is correct and shipped; the deployment is current. So the line-count "discrepancy" was a measurement-shape difference, not a build/deploy gap.
  
  The orphan audit is the substantive finding. `guardFlat` and `guardRich` ARE present in BrowserRenderer (`src/lib/playground/browser-renderer.ts:297-417`), ported line-for-line from `packages/pilcrow-typeset/src/playwright.ts:639-957`. The hypothesis that BrowserRenderer lacks the guard entirely is wrong — it runs on every paragraph in the playground exactly as it runs in PlaywrightRenderer at build. The cases the user sees broken are not a wiring gap; they are out-of-scope for the guard's design.
  
  `firstOrphanIdx` only fires on two conditions: (Case 1) line N ends with a literal `-` AND line N+1's leading word has fewer than `ORPHAN_THRESHOLD = 4` chars; (Case 2) line N ends with `-<suffix>` matching `/-([a-zA-ZÀ-ɏ]{1,7})$/` AND line N+1's leading word has fewer than `RIGHTMIN = 3` chars. The whole algorithm targets hyphenation orphans specifically — the post-hyphen residual of a SHY-driven mid-word break.
  
  Walking the seven concrete cases through this predicate: deployed Fraunces `t` alone (1 char, hyphenation orphan from "sentence") triggers Case 1 cleanly and the guard SHOULD recover via `findOrphanSHYPos` strip-and-relayout — that this case appears in the deployed output is anomalous and warrants its own probe. `cannot` alone (6 chars) does NOT trigger Case 1 because 6 > threshold 4 — by design. The threshold is the design ceiling, not a bug. `fake` alone (4 chars) does NOT trigger because the predicate is `nextLen < ORPHAN_THRESHOLD` (strict less-than), so exactly 4 fails the condition; widen to `<= 4` would catch this. `any-` alone is ambiguous from the brief — if it's the LAST line of a paragraph as a widow ending in literal hyphen, no case fires; if it's line N ending the float with a hyphen but no following residual, the guard skips because there's no line N+1. Playground broken-font cases: EB Garamond `the` (3 chars) and Spectral `two` (3 chars) are LAST-LINE WIDOWS — complete English words with no preceding hyphen on line N — and `firstOrphanIdx` returns -1 for them because Case 1 requires `t.endsWith('-')`. Inter `typo-` is the line-N-ends-in-hyphen pattern; whether it triggers depends on what's on line N+1 (likely "graphy", 6 chars, which exceeds threshold and won't fire).
  
  So the diagnosis splits cleanly. (a) Single deployed `t`-from-"sentence" is a guard MISS that should have been a hit — needs targeted probe to determine whether `findOrphanSHYPos` failed to find the SHY in the source text or whether the ragged-fallback exhausted retries. (b) The other deployed `fake`/`cannot` and the widow-class playground cases are OUT OF SCOPE for the guard as designed — the guard is named "Hyphenation orphan guard" and its docblock explicitly states "Detection: a line ends with a visible hyphen". Widow handling was never within its remit. (c) The Inter `typo-` playground case sits at the threshold boundary — depending on line N+1 length it may be in or out of scope, and the threshold is a deliberate design parameter (commented at `playwright.ts:669`: "THRESHOLD = 4: catches fail-|ure (right=3), ital-|ics (right=2); lets inely (5) pass").
  
  The unified-vs-split answer: BrowserRenderer parity with PlaywrightRenderer is INTACT for the orphan guard (same algorithm, same threshold, same predicates, same guard helpers, runs on every paragraph). The deployed Fraunces orphans and the playground broken-font orphans ARE NOT the same bug. The deployed `t`-singleton is a Case-1 escape that warrants investigation. The other deployed cases plus the playground widows are a DIFFERENT class — last-line-widow handling — that the guard was never written to address. The Inter `typo-` case probes the threshold boundary and is a candidate for threshold widening. Fonts with broader average glyph widths (Spectral, EB Garamond, Inter at the body sizes) produce shorter visual lines for the same text and a different break geometry, surfacing widows that Fraunces — narrower for the same content — happens not to produce in the same paragraph; this is glyph-metric divergence, not a renderer-host divergence.
  
  Fix-scope options. (i) Audit the deployed `t`-orphan to determine why the guard's SHY strip didn't recover; if `findOrphanSHYPos` fails, that's a new gap in the targeted-recovery algorithm worth fixing in both `playwright.ts` and `browser-renderer.ts` ~30-60 min. (ii) Widen `ORPHAN_THRESHOLD` from 4 to 6 in both renderers — closes deployed `fake` and `cannot` if they are post-hyphen residuals, ~30 min, but the doc-comment warns "lets inely (5) pass" implying widening triggers more aggressive SHY-stripping which can produce a ragged-right paragraph instead of a clean break; needs visual review of all posts after the change. (iii) Add a widow-handling layer (whole-word last-line check: if the last pt-line of a paragraph is a single complete word under N chars, attempt to pull the previous line's last word forward by re-running pretext with a slightly narrower width on the penultimate line) — net new code in both renderers, ~2-3 hours, structurally different from the hyphenation guard, would close the playground's EB Garamond/Spectral widow cases AND the deployed sidenote/example post `is`/`and`/`of`/`a` cases visible during this audit. (iv) Wait for upstream pretext #162 (`softHyphenMode: 'strict'`) — closes the hyphenation-class orphans architecturally; does NOT close widows; does NOT close cases above the current threshold. Upstream timeline unknown. (v) Combine (ii) + (iii) — widen threshold and add widow handling, ~3 hours total, closes the most user-visible breakage in both surfaces.
  
  Generalising to the pattern: every layer of the rendering pipeline (cap geometry per entry 6, font-loading lifecycle per entry 6, line breaking per pretext, orphan handling per this audit, hyphenation pre-pass per entries 1-2, glyph-metric drift per entry 6) must be audited for parity at every host where it runs, AND for completeness against the user-visible symptom class. "BrowserRenderer matches PlaywrightRenderer for the obvious metric (line counts, guard presence)" doesn't mean it matches for all symptoms a user sees, because the canonical algorithm itself can have an explicit, documented scope ceiling — and that ceiling is where the next class of visible bugs lives. The orphan guard is correctly canonical at both hosts; the gap is between the guard's hyphenation-orphan scope and the user's "line of single small word looks bad" perception. Fix the right layer for the right symptom class — not whichever layer happens to be the most-recently-shipped. Audit by symptom class first, by layer second.

  ADDENDUM 2026-05-08 evening: precision sweep against deployed DOM showed the orphan strings ('t', 'fake', 'cannot', 'any-') don't exist as standalone pt-line spans. They are end-words of long ~100-char pt-line spans being visually wrapped by the reader's browser at display time, NOT DOM-level orphans. The orphan-guard isn't the bug. The bug is downstream of typesetting: pre-computed pt-line spans are visually re-wrapping at reader-time despite being pre-broken at build-time. This refines the visual-verification-gap learning from entry 5: visual verification ALSO needs DOM corroboration, because what looks like a typeset short line might be a display-wrap of a typeset long line. The DOM is canonical; the screen is interpretation. Generalisation: every diagnostic claim about pt-line content must be paired with the actual DOM innerText, not the screenshot's interpretation of it.

- [2026-05-08] typography-architect: Upstream-first pays off. The orphan-guard wrapper was acknowledged technical debt from 2026-04-29 onwards, with the explicit plan being "don't fork pretext; file upstream and use the local guard while waiting." That plan ran for nine days. On 2026-05-08 the upstream maintainer (Cheng Lou) read the bug report, agreed it was a real bug, and shipped commit `f06fef0` as a default-behaviour fix — not the opt-in `softHyphenMode: 'strict'` flag the issue proposed, but a change in default behaviour (post-SHY grapheme packing removed unconditionally; 100 deletions, 2 additions in `src/line-break.ts`). The local guard now becomes dead code on the next pretext release.

  Generalisation: when working with active upstream maintainers, the value of a clean repro and well-framed bug report compounds. The "super clear repro and explanation" framing in Cheng Lou's reply suggests the report was action-shaped — short, specific, with corpus evidence. That's the shape upstream maintainers can act on without back-and-forth. Compare to vague reports that demand interactive triage; those die in inboxes.

  Strategic implication for Pilcrow's roadmap: the master plan §3 framing ("pretext is the architectural brain of Pilcrow") and §16 ("ride the wave; contribute upstream when needed") just paid for itself once. The orphan-guard removal will be the first instance of "upstream improvement absorbed by Pilcrow without local effort." Future upstream investments — SSR, canvas helpers, additional render targets per master plan §4 — should be treated the same way. File when needed, contribute repros, ride the upgrades.

  Pattern check before any future Pilcrow-local mitigation of an upstream limitation: file upstream first with a clean repro. Local mitigations are insurance, not solutions. The §3 "don't fork pretext" rule is more durable than it looked at first; this learning entry is its first concrete vindication.

- [2026-05-09] typography-architect: @shuding/opentype.js (the Satori fork) does NOT expose getBoundingBox() as a method — bounding box is available as direct glyph properties `xMin`, `xMax`, `yMin`, `yMax`. Always check the installed fork's API before using upstream opentype.js docs. The @shuding fork also doesn't ship @types — add a minimal ambient declaration in `src/types/` (pattern mirrors the existing `hyphenopoly.d.ts`).

- [2026-05-09] typography-architect: For build-time glyph rasterisation without adding the `canvas` npm package (a native dep), convert the opentype.js path commands to SVG `<path d="...">` and feed that SVG to Sharp. Sharp handles SVG rasterisation natively via libvips. The coordinate transform is: svgX = (fontX - xMin) * scale + padding; svgY = (yMax - fontY) * scale + padding (flips y-axis from font-up to SVG-down). This pattern avoids an extra native dependency and keeps the build pipeline consistent (Sharp is already a project dep).

- [2026-05-09] typography-architect: When passing per-row silhouette data (a number[] array) into Playwright's page.evaluate(), the array is serialised via JSON across the CDP boundary — this is correct and fast for arrays up to ~5000 elements (a 480px-tall obstacle has 480 entries). For very tall obstacles (>2000px), consider downsampling or streaming in batches. The current approach is fine for all editorial use cases.

- [2026-05-09] typography-architect: TypeScript strict-mode does NOT narrow union types (T | undefined) through if-guards inside closures. A pattern like `if (x !== undefined) { function foo() { use(x) } }` still reports `x` as possibly undefined inside `foo`. The fix: capture the non-optional value before the closure — `const xLocal = x; if (xLocal !== undefined) { function foo() { use(xLocal) } }`. This pattern is required for any closure defined inside an if-guard in strict mode.

- [2026-05-09] typography-architect: absolute filesystem paths embedded in build-time HTML data attributes (e.g. `data-src="/Users/author/..."`) leak the author's directory structure into the published HTML. For any attribute that carries a build-time filesystem path (as opposed to a URL), strip it from the final rendered output in playwright.ts after the Playwright pass has consumed it. The regex strip in the return path is the right place: `result.html.replace(/(<div[^>]+class="CONTAINER"[^>]*)\s+data-src="[^"]*"/g, '$1')`.
